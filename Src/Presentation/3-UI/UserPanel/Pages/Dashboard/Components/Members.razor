@using TaskoMask.Application.Share.Dtos.Workspace.Owners
@if (!Model.Any())
{
    <div class="alert alert-warning">
        <b>There isn't any member yet!</b>
        <hr>
        <small>
            You can assign new member to your organization by using
            <b>
                <a href="#" class="text-info">
                    <i class="bi bi-people"></i>
                    Add Member
                </a>
            </b>
            item from the top right menue
            <i class="bi bi bi-plus-circle"></i>
            . But make sure you have a project
        </small>
    </div>
}
else
{
    foreach (var item in Model)
    {
        <div class="col-md-4 col-sm-12 ">
            <div class="card" style="width: 18rem;">
                <img class="card-img-top" src="@defaultUserImgUrl" alt="Card image cap">
                <div class="card-body">
                    <h5 class="card-title">@item.DisplayName</h5>
                    <p class="card-text">@item.Email</p>
                    <a href="#" class="btn btn-sm btn-primary">Email</a>
                    <a href="#" class="btn btn-sm btn-warning">Permissions</a>
                    <a href="#" class="btn btn-sm btn-danger">Remove</a>
                </div>
            </div>
        </div>
    }
}

@code {

    private string defaultUserImgUrl = "/content/admin/img/default-user.png";

    [Parameter]
    public IEnumerable<OwnerBasicInfoDto> Model { get; set; }
}
