@model IEnumerable<TaskoMask.Application.Share.Dtos.Administration.Operators.OperatorBasicInfoDto>
@{
    ViewBag.Title = "Operators List";
}

<div class="grid-content">
    <div class="block">
        <div class="block-bar">
            <div class="form"">
                <a href="@Url.Action("Create", "Operators", new { })" class="btn btn-success">New Operatore</a>
            </div>
        </div>
        <div id="ajax-show-list">
            @await Html.PartialAsync("_ItemList", Model)
        </div>
    </div>
</div>


@section scripts{

}