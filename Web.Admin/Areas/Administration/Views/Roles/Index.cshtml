@model IEnumerable<TaskoMask.Application.Core.Dtos.Roles.RoleBasicInfoDto>

@{
    ViewBag.Title = "Roles List";
}

<div class="grid-content">
    <div class="block">
        <div class="block-bar">
            <div class="form">
                <a href="@Url.Action("Create", "Roles", new { })" class="btn btn-success">ایجاد نقش جدید</a>
            </div>
        </div>
        <div id="ajax-show-list">
            @await Html.PartialAsync("_ItemList", Model)
        </div>
    </div>
</div>


@section scripts{

}